spring:
  application:
    name: farm-monitoring-service
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://localhost:8765
      username: client
      password: 12345